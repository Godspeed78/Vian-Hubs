import React from 'react'
import './Loading.css';

const Loading = (props) => {
    return (
        <section className={'container loading'}>
            <div></div>
            <div></div>
            <div></div>
        </section>
    )
};

export default Loading
